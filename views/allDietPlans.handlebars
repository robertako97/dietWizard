<div class="container mt-5">
    <h2 class="text-center mb-4">Your Diet Plans</h2>
    <div class="list-group">
        {{#each dietPlans}}
            <a href="/get-plan/{{this.plan_id}}" class="list-group-item list-group-item-action">{{this.diet_type}}</a>
        {{/each}}
    </div>
</div>
